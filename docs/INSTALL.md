# Installation Laravel 8 SEO Pack FIXED

Copie tout le contenu dans ton projet Laravel 8.

Commandes :

```bash
composer update
php artisan migrate
php artisan db:seed
php artisan serve --host=0.0.0.0
```

URLs :

- /admin
- /blog
- /offres
- /realisations
- /sitemap.xml
- /sitemap-blog.xml
- /robots.txt
